@page "/projectStatuses/{id:int}"

@inject NavigationManager NavigationManager

<EditComponent TEntity="ProjectStatus" TView="ProjectStatusView" TContext="MyContext"
               Id="Id" @ref="editComponent">
    <MainTemplate>
        <EditForm EditContext="editComponent.EditContext">
            <DataAnnotationsValidator />

            <div class="input-group mb-3">
                <span class="input-group-text">Наименование</span>
                <InputText id="Name" @bind-Value="editComponent.Entity.Name" class="form-control" />
            </div>

        </EditForm>
    </MainTemplate>
</EditComponent>

@code {
    [Parameter]
    public int Id { get; set; }

    public EditComponent<ProjectStatus, ProjectStatusView, MyContext> editComponent { get; set; }
}
