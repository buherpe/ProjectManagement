namespace ProjectManagement
{
    //public class Class : BackgroundService
    //{
    //    protected override async Task ExecuteAsync(CancellationToken stoppingToken)
    //    {
    //        Console.WriteLine($"ExecuteAsync");

    //        while (true)
    //        {
    //            var context = new MyContext();
    //            var domains = context.Domains.Where(x => DateTime.Today < x.ExpiryDate &&
    //                                                     DateTime.Today.AddDays(30) > x.ExpiryDate);

    //            Console.WriteLine($"ExecuteAsync: {domains.Count()}");

    //            await Task.Delay(1000, stoppingToken);
    //        }
    //    }
    //}
}
